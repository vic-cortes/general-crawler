[project]
name = "general-crawler"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bs4>=0.0.2",
    "crawl4ai>=0.7.3",
]

[dependency-groups]
dev = [
    "ruff>=0.12.8",
]
